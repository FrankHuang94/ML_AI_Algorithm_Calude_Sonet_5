# 服务和队

这份文件涵盖了如何在生产中有效地计划和处理部署的LLM请求. 系统层层在此部分的其他所有内容 (量化,KV缓存管理,投机解码) 上面,并确定这些技术如何转化为现实世界的吞吐量和延迟.

## 通过与延迟的交易,框架

**吞吐量**服务系统可以处理每单位时间的请求 (或词元) 总数,在所有用户中. **延迟**根据用户的视角,一个单个请求需要多长时间才能得到答案.这两个目标经常紧张:将许多请求捆绑在一起 (同时处理) 往往会提高吞吐量 (加速器的平行计算能力的更好的利用 在一个 GPU 上一次运行一个请求,使其容量大大大不变) 但可以增加个人的延迟 (请求可能需要等待批量填充或与其他同步请求共享计算,才能完成).本文件中的大多数设计选择是,底部,同一交易曲线上的不同点.

## 静态批量

**机械.**收集一组固定的请求,从开始到结束将它们作为一批处理在一起,并且只在前一批完全完成后才开始新的批发 (其中的每个请求都产生了完整的响应).

**限制.**由于生成的响应长度很大 (有些响应是单句,其他是页面长),静态批量只有其速度.*最慢*成员  批量要求较短,但分配的计算槽停留无效,等待批量中最长的请求完成,然后系统可以开始在该槽中交付新的请求.这浪费了大量的潜在吞吐量,特别是随着响应长度差异的增长.

## 连续 (动态) 批量

**机械.**取代了等待整个批次完成之前开始新工作,不断监控批次,一旦其中的任何单个请求完成生成答案,立即交换到新的等待请求,以取出该释放的空隙批次的组成变化动态,要求按要求,而不是固定整个持续时间.

**为什么这很重要.**这直接解决了静态批量中心的效率低下:没有加速器容量停留在等待单个批量中最长的请求完成之前,因为完成的请求会立即取代而不是在固定批量周期结束时. 连续批量 (有时称为动态批量) 已被广泛报告以大幅提高可实现的服务吞吐量与静态批量相比,并且是现代高吞吐量LLM服务系统的标准,预期特征2026年.

**如何与KV缓存相互作用.**持续交换请求进入和出发的批量意味着服务系统需要有效分配和释放KV缓存 (见[kv-cache-and-attention-optimization.md](kv-cache-and-attention-optimization.md)对于单个请求即时进行,不需要重新组装或去断碎其他仍在进行的请求中使用的内存,这正是PagedAttention的区块/页面表设计 (包含在该文件中) 构建的有效解决问题.

## 参考系统:vLLM和TensorRT-LLM

**现在,我们要去做什么?**开源LLM服务系统 (引入PagedAttention的系统 见 [kv-cache-and-attention-optimization.md](kv-cache-and-attention-optimization.md)) 专门围绕连续批量和高效的页面化KV缓存存储管理构建,

**射器RT-LLM (NVIDIA)**一个服务/输入优化工具包,应用硬件特定编译和内核优化 (专门针对NVIDIA GPU架构) 以及持续批量和量化支持等技术,旨在最大化吞吐量和最小化延迟,特别用于NVIDIA硬件.

**为什么这些值得命名.**两种系统都是广泛使用的具体现实实实体例,它们基本上结合了本节所涵盖的每一种技术.[quantization.md](quantization.md)),高效的KV缓存管理,连续批量和 (在各种集成中) 投机解码 (见[speculative-decoding.md](speculative-decoding.md))                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               

## 如何与批量决策相互作用

连接该部分的核心通过线:KV缓存的内存足迹 (见[kv-cache-and-attention-optimization.md](kv-cache-and-attention-optimization.md)根据GQA的规范,可直接限制在固定内存预算内连接多少同步请求. 一个较大的每次请求KV缓存 (从较长的背景中,或从没有MQA/GQA式缓存尺寸缩小的架构) 意味着可以同时批量更少的请求,这直接减少了可实现的吞吐量. 这就是为什么GQA式架构选择 (在预训练时间决定),量化 (适用于模型,有时是KV缓存本身),以及PagedAttention式内存管理 (适用于服务时间) 都最终从不同的角度来实现相同的下游目标:将更有用的同步工作安装在昂贵的加速器固定的内存量中,这是储库中本质上每个部分的资源限制技术,以一种或另一种方式,试图放松.

## 比较表

|方法|吞吐量|短时间请求的延迟|如何处理变量响应长度?|
|---|---|---|---|
|静态批量|低 (等待最长的请求时空缺时间) |可能是穷 (短暂的请求等待整个批量) |没有|
|连续 (动态) 批量|高 (立即填充的槽) |更好 (完成的请求立即退出) |现在|

## 与其他算法的关系

- 连续批量效率增长直接由KV缓存存储管理实现[kv-cache-and-attention-optimization.md](kv-cache-and-attention-optimization.md).
- 量化 (见[quantization.md](quantization.md)) 减少每次请求的内存足迹,直接增加可以在固定内存预算内批量请求的数量.
- 投机解码 (见 [speculative-decoding.md](speculative-decoding.md)) 和批量是互补的吞吐力杆,这两种杆通常在生产服务堆中一起部署,如vLLM和TensorRT-LLM.

## 来源

- 等人",Orca:基于Transformer的生成模型的分布式服务系统" (2022) 被广泛认为是引入了 LLM服务的连续/代级分批
- 科恩等人",用页面注意力服务的大型语言模型的有效记忆管理" (2023) [鱼]
