# 变更日志

## v2 — Opus 审查和扩展通行证

对每个内容文件进行完整的第二次通行，以纠正任何技术上的不精确之处，加深解释，并使材料更容易理解。总内容从约 69,600 字增加到约 78,000 多个字，并且在整个内容中添加了手绘视觉效果 - 不是作为填充，而是为了使最难描绘的机制具体化。

**正确性/精度修复**
- 将自注意力的“排列不变”更正为基石 Transformer 文件中的精确术语 **排列等变**（并解释了区别）。
- 澄清了经典与标准化（EMA）**动量**公式，因此当论文的方程与框架的代码不匹配时，读者不会感到困惑。

**可理解性添加（工作示例 + ASCII/美人鱼视觉效果）**
- 在最有帮助的地方添加了具体的数字演练：梯度下降步骤、交叉熵损失值、注意力加权和、Q 学习更新（带有 TD 误差）、LoRA 的参数节省、INT4 量化“阶梯梯级”。
- 新图表：优化器族树、训练/验证损失曲线（过度拟合）、损失函数形状、每个分类器的决策边界形状、bagging-vs-boosting、同心环上的 k-means-vs-DBSCAN、EM 循环、注意力数据流、卷积边缘检测器、LSTM 传送带、MoE 路由、消息传递、GAN 对抗循环、扩散前向/反向、重新参数化梯度流、采样策略、RLHF 管道、并行类型、KV 缓存重新计算、推测性草稿验证、代理环境循环、PPO 裁剪。

**内容更新至 2026 年中期（真正的空白已填补）**
- 优化器：**Muon** (2024) 和 **WSD** 学习率计划。
- 正则化：**GroupNorm**、**QK-norm**、随机深度和 **隐式正则化** 部分（SGD 噪声、平坦最小值）。
- 损失：**Huber 损失**，以及**前向与反向 KL**（模式覆盖与模式搜索）区别。
- Transformers：**前馈子层**（以前仅在框图中）、门控激活（GELU/SwiGLU）以及与 KV 缓存相关的训练并行与生成顺序澄清。
- MoE：细粒度+共享专家（DeepSeek 式）。
- 扩散：**流量匹配/整流流量**、DDIM 和基于蒸馏的快速采样器。
- VAE：后塌陷、β-VAE 和 **VQ-VAE**（自回归图像/音频生成的桥梁）。
- 训练：**标记化/BPE**部分、**GRPO**（用于推理模型的无值模型 RL）和 DoRA/LoRA 系列。
- 经典机器学习：区分性与生成性框架预先明确。

**内务管理**
- 术语表增加到约 105 个术语，保持按字母顺序排列并与每次添加保持同步；术语计数表已刷新。
- 更新主比较表（Muon，流匹配）以实现跨文件一致性。
- 重新验证：所有内部链接均已解析，所有 14 个美人鱼图格式良好，每个文件至少有一个视觉效果。

## 构建完成 - ML/AI 算法知识库 v1

原始范围中描述的完整仓库构建已完成：一个全面的、索引的、基于 markdown 的参考，涵盖从经典统计方法到前沿 2026 LLM 训练和推理实践的 ML/AI 算法，以及它们的发展历史和近/长期展望。

**总内容：** 45 个内容文件中约 69,600 字（不包括本变更日志、贡献指南和进度草稿文件）——轻松超过了原始范围中的 50,000 字底线设置，这是通过真正的主题覆盖而不是填充来实现的。

### 交付的结构

- **`docs/00-overview/`** — 范围和方法（包括明确的受众校准）、按字母顺序排列的实时术语表（约 85 个术语）以及将每个部分联系在一起的分类图美人鱼思维导图。
- **`docs/01-foundations/`** — 优化算法（通过 AdamW/Lion/Sophia/LAMB 的梯度下降）、正则化技术、损失函数。
- **`docs/02-classical-ml/`** — 监督学习、集成方法、无监督学习、概率模型。
- **`docs/03-deep-learning-architectures/`** — CNN、RNN/LSTM/GRU、Transformer（基础文件，根据原始规范提供额外深度）、专家混合、状态空间模型、图神经网络。
- **`docs/04-generative-models/`** — GAN、VAE、扩散模型、自回归生成、基于流的模型。
- **`docs/05-training-methodology/`** — 预训练策略、微调/PEFT (LoRA/QLoRA)、RLHF 和对齐（完整管道加上 DPO/RLAIF/Constitutional AI）、分布式训练、课程和数据策略。
- **`docs/06-inference-optimization/`** — 量化、修剪/蒸馏、KV 缓存和注意力优化（FlashAttention/PagedAttention/GQA）、推测性解码、服务和批处理。
- **`docs/07-reinforcement-learning/`** — 基于价值的方法、策略梯度方法（深入 PPO）、基于模型的 RL 和专用的 RL-for-LLM 桥接文件。
- **`docs/08-history/`** - 四个按时间顺序排列的时间线文件（1950年代-2000年代、2000年代-2017年、2017-2023年、2023年至今），每个文件都有一个美人鱼时间线图，涵盖了当前RL-on-可验证奖励/代理训练时代的感知器。
- **`docs/09-roadmaps/`** - 近期（6-18个月）和长期（2-5年）展望，均带有 `[Projection]` 标签，将来源声明与猜测分开，加上一个涵盖灾难性遗忘、幻觉、可解释性、代理可靠性、计算成本和评估有效性的开放问题文件。
- **`docs/10-comparison-tables/`** — 仅横切综合：每个主要类别的主比较表、特定于计算成本的比较以及带有 Mermaid 决策树的实用决策指南。
- **`README.md`** — 完整链接的目录，每个文件有一行描述。

###

中遵循的约定 - 每个条目都遵循 `scope-and-methodology.md` 中定义的九部分每个算法模板（名称/定义、起源、机制、为什么重要、当前状态、优势/限制、今天的使用、关系、来源）。
- 每个文件至少包含一个美人鱼图或一个重要的比较表。
- 每个行话术语都是在首次使用时定义的，并在实时术语表中内联和交叉引用。
- 历史主张已注明日期并归因于特定论文；不确定的日期/属性被标记为此类，而不是陈述为事实。
- 路线图声明明显分为来源/记录的方向和 `[Projection]` 标签的猜测。
- 所有 681 个内部交叉链接均经过验证，可在最终一致性传递过程中正确解析。

### 已知限制

- 这是 2026 年中期的快照；根据仓库自己声明的方法，`08-history/timeline-2023-present.md` 和 `09-roadmaps/` 中的 RL-可验证奖励和代理培训材料是历史记录中最不稳定的部分，事后看来应该会有所不同。
- 根据 `scope-and-methodology.md` 中的“无发明精度”质量条，故意将基准数字作为定性比较或范围而不是精确数字给出。
